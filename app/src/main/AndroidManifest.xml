<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meghana.donations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignUpForm"
                    android:label="Signup"/>


        <activity android:name=".LoginForm"
                   android:label="Login"/>

        <activity android:name=".OrganisationForm"
                  android:label="Organisations"/>
        <activity android:name=".ListviewOrganisations"
                    android:label="Organisations"/>

        <activity android:name=".DonationActivity"
                   android:label="Donate"/>



    </application>

</manifest>